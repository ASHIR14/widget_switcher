name: widget_switcher
description: An animated widget that lets you switch between widgets.
version: 0.1.0

homepage: https://github.com/ASHIR14/widget_switcher
repository: https://github.com/ASHIR14/widget_switcher

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

platforms:
  android:
  ios:
